#include <iostream>
 using namespace std;



struct Pasajeros{

    char nombre[20];
    int Identificacion;
    int destino; // la bocana, pianguita, ladrilleros
    struct pasajeros*siguiente;


};

pasajeros* cab,*aux,*aux2;

void registrar()

aux = (struct pasajeros*) malloc (sizeof(Pasajeros));
cout<<"Ingrese el nombre Pasajero: ";
cin>>aux->nombre;
cout<<"Ingrese la Indentificacion: ";
cin>>aux->Identificacion;
cout<<"Ingrese el Destino del Pasajero: ";
cout<<'\t'<<"¿Que lugar desea visitar?"<<"Marque 1 si elije la bocana"<<'\t'<<"Marque 2 si elije pianguita"<<'\t'<<"Marque 3 si elije la ladrilleros"<<
cin>>aux->destino;
aux->siguiente = NULL;



(char nombre, int documento, int destino){



    if (nuevopasajero!=NULL){
    string (nuevopasajero->nombre);
    nuevopasajero->numero_documento=numero_documento;
    nuevopasajero->destino=destino;
    nuevopasajero->sig=NULL;
    
    
    }




    }

if (!cab)
    {
        cab = aux;
    }else{

        aux2 = cab;
        while (aux2->siguiente != NULL)
        {
            aux2 = aux2->siguiente;
        }

        aux2->siguiente = aux;
        aux = aux2 = NULL;
        free(aux);free(aux2);      
        
    }



void mostrar()
{
    aux = cab;
    int item = 0;
    if(cab != NULL)
    {
        cout<<"item"<<'\t'<<"Identificacion"<<'\t'<<"Nombre"<<'\t'<<"Destino"<<endl;
        while (aux != NULL)
        {
            item++;
            cout<<item<<'\t';
            cout<<aux->Identificacion<<'\t'<<'\t';
            cout<<aux->nombre<<'\t';
            cout<<aux->destino<<endl;
            auxiliar = aux->siguiente;
        }
        aux = NULL; free(aux);
    }

}

void ordenar(){}

int main()
{
    int opcion;
    do
    {
        cout<<'\t'<<"MENU"<<endl;
        cout<<"1. Ingresar Pasajero"<<'\n'<<"2. Mostrar lsita de  Pasajeros"<<'\n'<<"3. Ordenar lista de Pasajeros"<<endl;
        cout<<"0. salir"<<endl;
        cin>>opcion;

        switch (opcion)
        {
        case 1:{registrar();}
            break;
        
        case 2:{mostrar();}
            break;

        case 3:{ordenar();}
            break;
        }

    } while (opcion != 0);
        
}
